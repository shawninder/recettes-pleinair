import Page from '../components/Page'

export const meta = {
  title: `FAQ`,
  desc: 'Des réponses aux questions les plus fréquentes'
}

# FAQ

_à venir_

export default ({ children }) => <Page locale='fr' meta={meta}>{children}</Page>
